<%= form_for([:admin, @image], html: {multipart: true }) do |f| %>
<p><%= f.label :title %></p>
<p><%= f.text_field :title %></p>
<p><%= f.label :image %></p>
<p><%= f.file_field :avatar, as: :file_upload %></p>
<p><%= f.submit "Submit" %></p>
<% end %>